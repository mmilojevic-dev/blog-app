import styles from './PostDetails.module.css'

type PostDetailsProps = {}
export const PostDetails: React.FC<PostDetailsProps> = ({}) => {
  return <div className={styles.postDetails}>POST DETAILS</div>
}
